sudo: required
services:
  - docker

before_install:
  - docker build -t alexanderzhigalov/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true alexanderzhigalovgit /docker-react npm run test
